# 수수깡 프론트엔드

수공예 한정수량 판매 플랫폼 수수깡의 최소 프론트엔드. Vite + React, 백엔드(Spring Boot, `http://localhost:8080`)를 fetch로 직접 호출한다.

## 실행

```bash
npm install
npm run dev   # http://localhost:5173
```

백엔드에 CORS 설정이 없으므로, 설정 전까지 브라우저 콘솔에 CORS 에러가 나는 것이 정상이다(재현용).
